#### constexpr
해당 변수나 함수의 값을 가능한 경우 컴파일 타임에 계산하여 성능을 최적화하고, 컴파일 타임 상수가 필요한 문맥(배열 크기, 템플릿 인자)에서 사용할 수 있게 해주는 키워드이다.

`constexpr`로 선언된 변수나 함수는 컴파일 중에 값이 결정된다. `const`는 변수 값이 런타임에 결정될 수 있지만, constexpr은 반드시 컴파일 타임에 초기화되어야 한다.